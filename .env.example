# URL внешнего API, к которому будет обращаться бэкенд.
# По умолчанию используется https://httpbin.org/anything (эхо-ответ).
EXTERNAL_API_URL=https://api.ai.localcorp.net/

# Порт HTTP-сервера (по умолчанию 8080).
PORT=8080
